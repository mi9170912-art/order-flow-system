# نظام مسار الطلبات — خطوات الرفع (5 دقايق)

النظام ده جاهز بالكامل: سيرفر + واجهة + قاعدة بيانات. اللي عليك بس ترفعه على استضافة مجانية وتاخد الرابط.

## الخطوات

### 1) اعمل حساب على GitHub (لو معندكش)
روح على github.com واعمل حساب مجاني بالإيميل.

### 2) ارفع هذا المجلد على GitHub
- من صفحة GitHub الرئيسية اضغط **New repository**.
- سمّيه مثلاً `order-flow-system` واضغط **Create repository**.
- في الصفحة اللي هتظهر، اضغط **uploading an existing file**.
- اسحب كل ملفات هذا المجلد (server.js, package.json, data.json, مجلد public) وارفعها، واضغط **Commit changes**.

### 3) اعمل حساب على Render (مجاني)
روح على render.com وسجل حساب — الأسهل إنك تسجل بحساب GitHub بتاعك عشان يتربطوا مباشرة.

### 4) اربط المشروع
- من لوحة Render اضغط **New** ثم **Web Service**.
- اختار الـ repository اللي رفعته (`order-flow-system`).
- في الإعدادات:
  - **Build Command:** `npm install`
  - **Start Command:** `npm start`
  - **Instance Type:** اختار **Free**
- اضغط **Create Web Service**.

### 5) خلاص
بعد دقيقة أو اتنين هيديك Render رابط زي:
`https://order-flow-system.onrender.com`

الرابط ده افتحه من أي موبايل أو تابلت أو كمبيوتر — النظام هيشتغل بنفس البيانات على كل الأجهزة فورًا.

---

## ملاحظة مهمة عن الباقة المجانية

الباقة المجانية في Render:
- بتنام لو محدش استخدمها لمدة 15 دقيقة (أول طلب بعد النوم ممكن ياخد نص دقيقة لحد ما يصحى — ده طبيعي).
- التخزين مؤقت (ephemeral) — يعني لو عملت تحديث جديد للكود أو السيرفر اتقفل فترة طويلة جدًا، البيانات ممكن تتصفّر.

**لو عايز البيانات تفضل محفوظة دايمًا بدون أي مخاطرة:**
- ترقّي الـ Instance Type لـ **Starter** (حوالي 7 دولار شهريًا) وتضيف **Persistent Disk** من إعدادات الخدمة (حوالي 1 دولار شهريًا لمساحة صغيرة) — وده هيخلي الملف `data.json` يفضل محفوظ دايمًا مهما حصل.
- ده اختياري، وممكن تبدأ بالمجاني وتشوف الاستخدام الفعلي الأول.

---

## لو حبيت اسم دومين خاص بيك (مش onrender.com)
من إعدادات الخدمة في Render فيه خيار **Custom Domain** — تحط فيه الدومين اللي اشتريته (من Namecheap أو GoDaddy مثلاً) وتتبع التعليمات اللي هتظهرلك هناك.
